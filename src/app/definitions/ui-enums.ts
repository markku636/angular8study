export enum CampaignStatus {
    Active,
    Inactive,
    Suspended,
    Delete,
    Published,
    UnPublish
}

export enum CampaignType {
    RET,
    REG,
    DEP
}

export enum CampaignTriggerType {
    BET,
    REG,
    DEP
}

export enum Currency {
    GBP,
    EUR
}

export enum CampaignDateRangeType {
    Starts_between,
    Ends_between,
}
export enum Channel {
    Desktop,
    MobileWeb,
    MobileIOS,
    MobileAndroid,
}

export enum Country {
    UK,
    Eire
}

export enum Language {
    English,
    French,
    German
}


